spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1700000
      keepalive-time: 300000
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        format_sql: false

jwt:
  expiration: 3600000

server:
  port: 8080
  # CloudFront 가 TLS 종단 → 앱은 HTTP 로 받는다. 프록시가 X-Forwarded-* 를 전달하면 신뢰해
  # request.isSecure()/스킴 인식을 교정 — HSTS 등 HTTPS 조건 헤더의 발화 전제 (전구간 HTTPS 전환 대비).
  forward-headers-strategy: framework
  tomcat:
    accept-count: 100
    max-connections: 8192
    threads:
      max: 200
      min-spare: 10

ai:
  provider: gemini

# 운영 API 명세는 별도 관리 → prod 에서 Swagger UI·api-docs 비노출(공격 표면 축소).
springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    enabled: false

# prod 는 Swagger 가 없으므로 strict CSP(default-src 'none'). JSON 응답만 내므로 무영향.
app:
  security:
    content-security-policy: "default-src 'none'; frame-ancestors 'none'; base-uri 'none'; form-action 'none'"

logging:
  level:
    root: WARN
    com.example.tonefitserver: INFO
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n"
